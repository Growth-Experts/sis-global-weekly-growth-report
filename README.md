# SIS Global — Weekly Growth Report

Client-facing HTML deck for the weekly growth report.

## Local preview

```bash
npm install
npm start
```

Open the URL shown in the terminal (defaults to port 3000).

## Deploy

Pushed to GitHub and served on Railway. The public site root is `index.html`.
